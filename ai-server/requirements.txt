flask==3.1.0
flask-cors==4.0.2
python-dotenv==1.0.1
requests==2.31.0
supabase==2.0.3

# Build tooling (helps avoid build-time issues on some environments)
wheel==0.45.1
setuptools<81

# Core dependencies for faster-whisper (lighter than openai-whisper)
numpy>=1.21.0
av>=10.0.0
cython>=0.29.0

# Speech-to-text - faster-whisper is primary, openai-whisper is fallback
faster-whisper==1.1.1
# openai-whisper is optional and can be commented out to avoid build issues
# openai-whisper==20231117

# Production server
gunicorn==21.2.0


