flask==3.1.0
flask-cors==4.0.2
python-dotenv==1.0.1
requests==2.31.0
supabase==2.0.3

# Build tooling (helps avoid build-time issues on some environments)
wheel==0.45.1
setuptools<81

# Speech-to-text (note: these can be heavy and may require FFmpeg + a compatible PyTorch build)
openai-whisper==20240930
faster-whisper==1.1.1

# Production server
gunicorn==21.2.0


